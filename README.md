# Esha Permana — GitHub Pages Portfolio

Personal portfolio for Cloud-Native and Platform Engineering projects.

## Local preview

Open `index.html` in a browser, or serve the folder with any simple static HTTP server.

## GitHub Pages

Create a repository named:

`<your-github-username>.github.io`

Then push these files to the `main` branch and enable GitHub Pages from the repository settings.

## Before publishing

Replace the placeholder GitHub and LinkedIn URLs in `index.html` with your actual profiles.

## Featured project

The first featured project is designed around the Analytic Dashboard Showcase:
FastAPI + React + ClickHouse + OpenShift + Helm + Tekton + GitHub Actions.
